# MinIO Configuration
# Copy this file to .env.minio and update the values

# Root User Credentials (CHANGE THESE!)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme123!

# MinIO Region
MINIO_REGION=us-east-1

# Console Browser
MINIO_BROWSER=on

# Domain (for virtual-host-style requests)
MINIO_DOMAIN=localhost

# Browser Redirect URL
MINIO_BROWSER_REDIRECT_URL=http://localhost:9001

# Optional: Notify webhook for events
# MINIO_NOTIFY_WEBHOOK_ENDPOINT=http://your-app:8000/webhook/minio
# MINIO_NOTIFY_WEBHOOK_ENABLE=on

# Optional: Enable TLS (requires certificates)
# MINIO_OPTS=--certs-dir /root/.minio/certs